import React from 'react';

const LaborPlanner = ({ currentUser }) => {
  return (
    <div>
      <h1>Labor Planner</h1>
      <p>Ready for your requirements...</p>
    </div>
  );
};

// For compatibility with existing imports
export const LaborManagementSystem = LaborPlanner;

export default LaborPlanner;