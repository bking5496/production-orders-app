{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp",
        "--headless",
        "--host", "localhost",
        "--port", "3001",
        "--isolated"
      ],
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
}